# 张氏相机标定及畸变校正验证桌面软件 — 用户手册

---

## 一、系统简介

### 1.1 背景

在工业视觉、图像测量与质检等领域，相机镜头普遍存在径向与切向畸变，直接影响测量精度。为获得可靠的像素与物理尺寸对应关系，需先对相机进行标定，得到内参矩阵与畸变系数，再对采集图像进行去畸变处理。张正友平面棋盘格标定法因其操作简便、精度稳定而被广泛采用。本软件围绕该算法，将标定、畸变矫正与结果验证整合为一体化桌面应用，支持从标定数据准备、角点检测、内参求解到矫正验证与报告导出的完整流程。

### 1.2 简介

本软件全称为**张氏相机标定及畸变校正验证桌面软件**（英文名：Zhang Calibration），是一款基于张正友平面标定法的相机标定与畸变校正验证桌面应用。软件提供图形化界面，支持通过棋盘格图像完成相机内参与畸变系数的计算，并利用标定结果对图像进行去畸变；同时在固定裁剪区域内进行格点检测与尺寸验证，量化矫正效果，并可生成包含矫正前后对比图与检测数据的 Excel 报告，便于质量记录与追溯。

### 1.3 目标用户

- 工业视觉与图像测量工程师：需对相机进行标定并在产线或实验室中验证矫正效果；
- 质检与计量人员：需对广角/鱼眼镜头矫正结果进行可追溯的量化评估；
- 科研与教学人员：需完成相机标定实验或演示标定与矫正流程。

### 1.4 主要功能

- **相机标定**：从多张棋盘格图像中检测角点，计算相机内参矩阵与畸变系数（k1、k2、p1、p2、k3），并显示平均重投影误差（RMS）；
- **标定结果管理**：将标定结果保存为 NPZ 或通过导出得到 TXT 可读格式，便于与其它系统对接；
- **畸变矫正**：使用标定得到的参数对单张或批量图像进行去畸变；
- **矫正验证**：在矫正前/矫正后图像上按统一规则截取裁剪区域，检测棋盘格点并计算格子物理尺寸，统计最小/最大像素当量及与标准值的偏差；
- **报告输出**：生成包含矫正前/后对比图、检测项、标准值、检出值、容差与是否通过的 Excel 报告。

### 1.5 优势与创新

- **一体化流程**：标定、矫正、验证与报告生成在同一界面内完成，无需切换多款工具；
- **增强角点检测**：采用预处理（如高斯模糊、直方图均衡、CLAHE 等）结合 OpenCV 的 findChessboardCornersSB，失败时自动回退经典方法并做亚像素精化，提高在光照不均、部分遮挡等条件下的检测成功率；
- **统一裁剪验证**：在矫正前/后图像上按横纵 1/5 与 4/5 确定同一裁剪区域，在该区域内进行格点检测与物理尺寸计算，使矫正效果可量化、可对比；
- **多格式图像支持**：支持 JPG、PNG、HEIC 等常见格式，便于直接使用手机或相机原始输出；
- **桌面应用形态**：基于 PyQt5 的图形界面，支持 Windows、macOS，并可打包为独立可执行程序（如 macOS 的 .app），便于部署与分发。

---

## 二、系统运行环境

### 2.1 系统硬件环境

- **处理器**：x86 或 ARM 架构的通用计算机（如 Intel/AMD 台式机、笔记本，或 Apple Silicon Mac）；
- **内存**：建议 4GB 及以上；
- **存储**：预留一定磁盘空间用于标定图片、标定结果文件及生成的报告；
- **显示**：支持图形界面的显示器，建议分辨率 1280×720 及以上以便完整显示主窗口；
- **外设**：如需现场采集标定图像，需相机或带摄像头的设备；软件本身不强制要求摄像头，可使用已有图片进行标定与验证。

### 2.2 系统软件环境

- **操作系统**：Windows 10/11，或 macOS 10.14 及以上；
- **运行依赖**（以 Python 方式运行时）：
  - Python 3.8 或更高版本；
  - OpenCV（opencv-python）4.5.0 及以上；
  - NumPy 1.19.0 及以上；
  - PyQt5 5.15.0 及以上；
  - Pillow 9.0.0 及以上（用于多格式图像读取）；
  - pillow-heif（可选，用于 HEIC 图像）；
  - PyQt-Fluent-Widgets（可选，用于 Fluent 风格界面）；
  - openpyxl 3.0.0 及以上（用于生成 Excel 报告）。
- 用户可通过项目提供的 `requirements.txt` 使用 `pip install -r requirements.txt` 安装上述依赖。若使用已打包的独立应用（如 .app），则无需单独安装 Python 与依赖库。

---

## 三、系统主要功能

本软件主界面由顶部功能切换与下方内容区组成。顶部提供两个功能入口：「标定与结果」与「畸变校正验证」。以下按界面结构逐项说明各功能。

---

### 3.1 主窗口与功能切换

- **窗口标题**：主窗口标题为「张氏相机标定 - Zhang Calibration」，支持设置应用图标（如已配置）。
- **最小尺寸**：窗口最小尺寸为 1200×740 像素，以保证各区域完整显示。
- **功能切换**：
  - **「标定与结果」**：点击后进入标定与结果页，用于选择标定图片、设置棋盘格参数、执行标定并查看/导出标定结果。
  - **「畸变校正验证」**：点击后进入畸变校正验证页，用于加载标定文件、选择验证图片、进行矫正与格点验证，并下载矫正图或生成 Excel 报告。
  - 当前选中的功能以高亮样式显示，两个入口互斥选择。

---

### 3.2 「标定与结果」页功能说明

本页采用左右分栏布局：左侧为标定图片列表与目录选择，中间为图像预览与检测状态，右侧为棋盘格参数、运行日志与结果报表。

#### 3.2.1 标定图片区域（左侧卡片）

- **标题与说明**：显示「标定图片」标题及「选择包含棋盘格的图片目录」说明。
- **「选择标定图片目录」按钮**：点击后弹出目录选择对话框，用户选择存放棋盘格图像的文件夹。软件会扫描该目录下符合扩展名（.jpg、.JPG、.png、.PNG、.HEIC、.heic）的图片并加载到列表。
- **当前目录显示**：选择目录后，下方标签会显示所选目录的完整路径；未选择时显示「未选择目录」。
- **图片列表**：以列表形式展示目录内所有支持的图片，每项显示文件名及缩略图（若有）。用户点击某一项时，中间预览区会显示该图片；若已完成标定且参数有效，预览区会显示角点检测结果（角点叠加显示）。
- **交互说明**：列表支持滚动浏览；点击不同图片即可切换预览与检测状态显示。

#### 3.2.2 图像预览与检测状态（中间区域）

- **预览区标题**：提示「图像预览（滚轮缩放；点击「开始标定」后显示角点）」。
- **图像预览控件**：
  - 支持**鼠标滚轮缩放**：滚轮向上放大、向下缩小，便于查看细节；
  - 支持**鼠标拖拽平移**：按住左键拖拽可平移查看大图；
  - 初始状态下显示「还未上传图像」；选择目录并选中图片后显示对应图像；若该图无法读取，则显示「无法读取图像」。
- **角点显示逻辑**：仅在用户点击「开始标定」且标定成功完成后，再在预览中根据当前棋盘格参数对选中图片进行角点检测并绘制角点；标定完成前或参数无效时，预览仅显示原图或提示「请先点击「开始标定」后显示角点」/「请设置有效棋盘格参数」。
- **检测结果标签**：预览区下方有一行检测状态文字，如「检测结果: --」「检测结果: 已检测到角点」「检测结果: 未检测到角点」「检测结果: 无法读取图像」等，用于快速了解当前图片的角点检测情况。

#### 3.2.3 棋盘格参数（右侧卡片）

- **内角点列数**：数字输入框，取值范围 0～20，表示棋盘格**内角点**的列数（即黑白格相交点的列数），需与实物棋盘格一致。
- **内角点行数**：数字输入框，取值范围 0～20，表示棋盘格**内角点**的行数。
- **格子边长 (mm)**：浮点数输入框，取值范围 0～100，单位毫米，表示棋盘格每个格子的实际边长，用于将像素距离转换为物理尺寸。
- **校验规则**：点击「开始标定」时，软件会校验内角点列数、行数均不小于 3，且格子边长大于 0，不满足时会弹出提示。

#### 3.2.4 开始标定与进度

- **「开始标定」按钮**：在已选择标定图片目录且棋盘格参数有效的前提下，点击后启动标定流程。标定在后台线程执行，界面不阻塞。
- **进度显示**：标定进行时，中间预览区会显示「正在处理图片...」及动态进度条；右侧进度条同步显示处理进度。完成后进度条隐藏。
- **运行日志**：右侧「运行日志」卡片内为只读文本框，实时输出标定过程的文字信息（如找到的图片数量、每张图的角点检测结果、内参矩阵、畸变系数、平均重投影误差等）。用户可滚动查看完整日志。

#### 3.2.5 结果报表与导出

- **结果报表卡片**：标定成功后，右侧「结果报表」表格中会显示主要标定结果，包括：
  - **fx、fy**：焦距（像素）；
  - **cx、cy**：主点坐标（像素）；
  - **k1、k2、p1、p2、k3**：畸变系数；
  - **平均重投影误差 (像素)**：RMS 重投影误差。
- **「下载 npz 到本地」按钮**：标定完成后该按钮变为可用。点击后弹出保存文件对话框，用户可选择路径与文件名，将当前标定结果保存为 NumPy 的 .npz 格式，便于后续在「畸变校正验证」页加载或供其他程序使用。未完成标定时按钮不可用。

---

### 3.3 「畸变校正验证」页功能说明

本页用于加载已有标定结果、选择待验证图片、执行去畸变与格点验证，并支持查看矫正前后对比、下载矫正图与生成 Excel 报告。

#### 3.3.1 标定与图片选择（顶部控制栏）

- **「加载标定 (npz)」按钮**：点击后弹出文件选择对话框，用户选择此前保存的标定文件（.npz 格式）。加载成功后，下方标签显示该文件路径；未加载时显示「未加载标定」。支持从「train_result/txt_result」等默认路径快速定位。
- **「选择验证图片目录」按钮**：点击后弹出目录选择对话框，用户选择存放待验证图像的文件夹。软件会扫描该目录下支持的图片格式并填入「当前图片」下拉框。选择目录后，下方标签显示目录路径；未选择时显示「未选择目录」。切换或重新选择目录会清空之前的验证结果列表，并影响「生成矫正报告 (Excel)」的可用状态。

#### 3.3.2 当前图片与显示模式

- **「当前图片」下拉框**：列出当前验证图片目录下的所有图片文件名。用户选择某一项即切换为对该图片进行矫正与验证；切换后会自动重新执行矫正与格点验证。
- **「显示」下拉框**：用于切换左右两侧图像区域的显示内容。
  - **「整图（原图 / 矫正图）」**：左侧显示当前图片的原始图像，右侧显示该图经标定参数矫正后的整幅图像。
  - **「裁剪验证图（矫正前 / 矫正后）」**：左侧显示矫正前图像在固定裁剪区域（横纵 1/5 与 4/5 围成区域）内的裁剪图并带格点与标注，右侧显示矫正后同一区域的裁剪验证图。便于直接对比矫正前后同一区域的格点与尺寸。

#### 3.3.3 矫正前/矫正后图像区域

- **左侧图像区**：标题为「矫正前」。在「整图」模式下显示当前选择的原图；在「裁剪验证图」模式下显示带格点与标注的矫正前裁剪图。支持滚轮缩放与拖拽平移。
- **右侧图像区**：标题为「矫正后」。在「整图」模式下显示当前图的矫正结果整图；在「裁剪验证图」模式下显示带格点与标注的矫正后裁剪图。同样支持滚轮缩放与拖拽平移。
- **处理中状态**：矫正与验证进行时，两侧区域会显示「正在处理图片...」及进度条；完成后自动刷新为对应图像或裁剪验证图。

#### 3.3.4 格点验证结果区域

- **标题**：「格点验证结果（横纵 1/5 与 4/5 裁剪区域）」。
- **状态标签**：处理过程中会短暂显示当前步骤说明（如「正在矫正与验证…」）；完成后隐藏。
- **验证结果文本**：以多行文字形式展示当前图片的验证摘要，包括裁剪区域内格点检测结果、最小/最大像素当量（或物理尺寸）、差值及与标准值的比较等，具体格式由底层验证逻辑输出。
- **「下载矫正后图片到本地」按钮**：完成当前图片的矫正与验证后可用。点击后先弹出目录选择对话框，用户选择保存路径；确认后软件将当前图片的矫正结果图、矫正前裁剪验证图、矫正后裁剪验证图（若存在）保存到该目录，文件名包含基名与后缀（如「xxx_undistorted.jpg」「xxx_crop_original_verify.jpg」「xxx_crop_undistorted_verify.jpg」）。未完成或失败时按钮不可用。

#### 3.3.5 格子边长与容差参数

- **格子边长 (mm)**：浮点数输入框，范围 0.1～100，单位毫米。表示验证时使用的棋盘格标准格子边长，用于将检测到的像素距离换算为物理尺寸并与标准值比较。修改后若已加载标定且已选图片，会自动重新执行矫正与验证。
- **容差 (mm)**：浮点数输入框，范围 0.01～10，单位毫米。表示判定「通过」时允许的检出值与标准值之间的最大偏差；用于验证结果判定与 Excel 报告中「是否通过」列。

#### 3.3.6 生成矫正报告 (Excel)

- **「生成矫正报告 (Excel)」按钮**：当至少有一张图片已完成矫正与验证（即验证结果列表中已有记录）时可用。点击后弹出保存文件对话框，用户选择路径与 .xlsx 文件名。确认后软件生成包含以下内容的 Excel 报告：
  - **工位**：对应图片名称（如文件名去掉扩展名）；
  - **矫正前图 / 矫正后图**：插入该图片的矫正前、矫正后裁剪验证图（缩略图）；
  - **检测项**：如「广角畸变」；
  - **标准值 (mm)**：当前设置的格子边长；
  - **矫正前检出值 (mm)**：矫正前裁剪区域内最大格子长度等；
  - **矫正后检出值 (mm)**：矫正后裁剪区域内最大格子长度；
  - **差值 (mm)**：矫正后检出值与标准值之差的绝对值；
  - **容差 (mm)**：当前容差设置；
  - **是否通过**：根据差值是否小于等于容差显示「通过」或「不通过」，并通过单元格背景色（如绿色/红色）区分。
- 报告生成过程中会在保存路径旁创建临时图片目录用于插入图片，完成后保存为 .xlsx 文件。若无验证结果可汇总，按钮不可用，点击会提示「暂无验证结果可生成报告」。

---

### 3.4 界面通用行为与提示

- **弹窗提示**：软件在关键操作完成或出错时会弹出信息框、警告框或错误框（如标定完成、请先选择目录、参数无效、保存成功、生成报告失败等），便于用户确认结果或按提示修正操作。
- **图像控件统一行为**：所有图像预览/展示区域均支持鼠标滚轮缩放与左键拖拽平移，便于查看细节与整体。
- **异步处理**：标定与矫正验证均在后台线程执行，界面保持响应；用户可在标定或验证进行中查看界面其他内容，但应避免重复点击「开始标定」或频繁切换图片导致多次触发任务。

---

以上内容覆盖本软件主窗口及「标定与结果」「畸变校正验证」两页中所有 UI 元素与功能，可作为用户操作与软著申请所用软件手册的功能描述部分。实际提交时可根据登记机构对页数或格式的要求进行排版或节选。
